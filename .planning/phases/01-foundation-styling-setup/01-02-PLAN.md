---
phase: 01-foundation-styling-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
  - src/main.jsx
  - src/App.css
  - src/assets/react.svg
autonomous: true
requirements:
  - REQ-003
  - REQ-004

must_haves:
  truths:
    - "src/App.jsx renders a minimal shell with no scaffold content (no counter, no Vite/React logos)"
    - "src/App.jsx does not import App.css or any deleted files"
    - "src/App.css is deleted"
    - "src/assets/react.svg is deleted"
    - "Inter font is loaded at weights 400, 500, and 700 via @fontsource/inter npm package"
    - "App.jsx root element is a <main> tag, giving the page a semantic landmark for REQ-004 anchor navigation"
  artifacts:
    - path: "src/App.jsx"
      provides: "Minimal portfolio shell — blank canvas for Phase 2+ sections"
      contains: "min-h-screen bg-white text-black"
    - path: "src/main.jsx"
      provides: "Entry point with Inter font imports"
      contains: "@fontsource/inter"
  key_links:
    - from: "src/main.jsx"
      to: "@fontsource/inter"
      via: "npm import for self-hosted Inter font"
      pattern: "import '@fontsource/inter"
    - from: "src/App.jsx"
      to: "Tailwind utility classes"
      via: "className on <main> element"
      pattern: "className=.*min-h-screen"
---

<objective>
Replace the Vite scaffold in src/App.jsx with a minimal portfolio shell. Install Inter font via Fontsource and import it in main.jsx. Delete the scaffold files App.css and assets/react.svg that are no longer needed.

Purpose: Clears the Vite default content so Phase 2+ sections can be built on a blank canvas. Ensures no deleted files are still imported (which would crash the dev server). Establishes Inter font loading.
Output: Clean App.jsx with semantic <main> shell, Fontsource Inter imports in main.jsx, App.css and react.svg deleted.
</objective>

<execution_context>
@/Users/luis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-styling-setup/01-RESEARCH.md
</context>

<interfaces>
<!-- Current state of files being modified. Executor should understand the existing code before replacing it. -->

Current src/App.jsx (scaffold — to be replaced entirely):
```jsx
import { useState } from 'react'
import reactLogo from './assets/react.svg'
import viteLogo from '/vite.svg'
import './App.css'

function App() {
  const [count, setCount] = useState(0)
  return (
    <>
      <div>...</div>
      <h1>Vite + React</h1>
      <div className="card">...</div>
    </>
  )
}
export default App
```

Current src/main.jsx (keep existing imports, add font imports after them):
```jsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.jsx'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install Fontsource Inter and add font imports to main.jsx</name>
  <files>src/main.jsx</files>
  <action>
Install the Inter font via Fontsource (self-hosted, no external network request):

```bash
npm install @fontsource/inter
```

Then update src/main.jsx to import three font weight CSS files. Add the imports after the existing imports, before the createRoot call:

```jsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.jsx'
import '@fontsource/inter/400.css'
import '@fontsource/inter/500.css'
import '@fontsource/inter/700.css'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

These three imports cover the font weights used throughout the portfolio: 400 (body), 500 (medium), 700 (bold/headings). The tailwind.config.js in Plan 01 already references 'Inter' as the first font in fontFamily.sans — these imports make the font available.

Do not change any other part of main.jsx. Do not add a default export.
  </action>
  <verify>
    <automated>node -e "import('fs').then(fs => { const content = fs.readFileSync('src/main.jsx', 'utf8'); const has400 = content.includes('@fontsource/inter/400.css'); const has500 = content.includes('@fontsource/inter/500.css'); const has700 = content.includes('@fontsource/inter/700.css'); const ok = has400 && has500 && has700; console.log(ok ? 'PASS' : 'FAIL: main.jsx missing fontsource imports'); process.exit(ok ? 0 : 1); })"</automated>
  </verify>
  <done>@fontsource/inter is installed in node_modules. src/main.jsx imports 400.css, 500.css, and 700.css from @fontsource/inter. All existing imports (StrictMode, createRoot, index.css, App) are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Replace App.jsx scaffold and delete unused scaffold files</name>
  <files>src/App.jsx, src/App.css, src/assets/react.svg</files>
  <action>
Step 1 — Replace src/App.jsx with the minimal portfolio shell. The current App.jsx imports App.css (which we're deleting) and several unused scaffold items. Write this exact content to src/App.jsx:

```jsx
function App() {
  return (
    <main className="min-h-screen bg-white text-black">
      {/* Portfolio sections will be added in Phase 2+ */}
    </main>
  );
}

export default App;
```

Critical: The new App.jsx must NOT contain:
- `import './App.css'` — App.css is being deleted; keeping this import crashes the dev server with "Failed to load url /src/App.css"
- `import { useState } from 'react'` — not needed
- `import reactLogo from './assets/react.svg'` — react.svg is being deleted
- `import viteLogo from '/vite.svg'` — not needed (vite.svg in /public stays, but we don't import it)
- Any scaffold JSX (counter, logos, "Vite + React" heading)

The root element is `<main>` — this is semantic HTML (REQ-061) and provides the page's primary landmark. The `id` attributes for anchor navigation (#about, #projects, etc.) will be added to child sections in Phase 2+. Using `<main>` as root satisfies REQ-004's minimal Phase 1 requirement.

Tailwind classes used:
- `min-h-screen` — page fills at least full viewport height
- `bg-white` — white background (design constraint: black/white only)
- `text-black` — default text color

Step 2 — Delete src/App.css:
```bash
rm src/App.css
```

Step 3 — Delete src/assets/react.svg:
```bash
rm src/assets/react.svg
```

Note: public/vite.svg is left in place (harmless, cleaned up in Phase 7).
  </action>
  <verify>
    <automated>node -e "import('fs').then(fs => { const appContent = fs.readFileSync('src/App.jsx', 'utf8'); const hasAppCssImport = appContent.includes('./App.css'); const hasScaffold = appContent.includes('useState') || appContent.includes('reactLogo') || appContent.includes('Vite + React'); const hasMain = appContent.includes('<main'); const appCssExists = fs.existsSync('src/App.css'); const reactSvgExists = fs.existsSync('src/assets/react.svg'); const ok = !hasAppCssImport && !hasScaffold && hasMain && !appCssExists && !reactSvgExists; console.log(ok ? 'PASS' : 'FAIL: ' + JSON.stringify({hasAppCssImport, hasScaffold, hasMain, appCssExists, reactSvgExists})); process.exit(ok ? 0 : 1); })"</automated>
  </verify>
  <done>src/App.jsx contains only the minimal shell with no scaffold imports. src/App.css does not exist. src/assets/react.svg does not exist. The App function returns a <main> element with Tailwind classes bg-white, text-black, min-h-screen.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the scaffold is fully replaced:

1. src/App.jsx — no imports of deleted files, no scaffold JSX
2. src/main.jsx — has @fontsource/inter imports at 400, 500, 700
3. src/App.css — file does not exist
4. src/assets/react.svg — file does not exist
5. @fontsource/inter appears in package.json dependencies (npm install ran)
</verification>

<success_criteria>
- src/App.jsx exports a function App that returns a semantic <main> element with Tailwind classes
- No import of App.css, useState, reactLogo, or viteLogo in App.jsx
- @fontsource/inter installed; main.jsx imports 400, 500, 700 weights
- App.css deleted
- src/assets/react.svg deleted
- No module resolution errors would occur when running npm run dev
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-styling-setup/01-02-SUMMARY.md` documenting:
- Final content of src/App.jsx and src/main.jsx
- Confirmation that App.css and react.svg were deleted
- @fontsource/inter version installed
- Any deviations from the plan
</output>
