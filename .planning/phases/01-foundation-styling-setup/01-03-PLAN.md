---
phase: 01-foundation-styling-setup
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified: []
autonomous: false
requirements:
  - REQ-001
  - REQ-002
  - REQ-003
  - REQ-004

must_haves:
  truths:
    - "npm run dev starts without errors"
    - "Browser shows a white page with no Vite scaffold content"
    - "A Tailwind utility class applied to a test element renders correctly (large, bold, black text)"
    - "Inter font loads (text renders in Inter, not system-ui fallback)"
    - "No blue links or dark backgrounds visible (Vite scaffold CSS fully replaced)"
  artifacts:
    - path: "tailwind.config.js"
      provides: "Tailwind config — must already exist from Plan 01"
    - path: "src/App.jsx"
      provides: "Minimal shell — must already exist from Plan 02"
  key_links:
    - from: "Browser"
      to: "Dev server"
      via: "npm run dev — confirm zero startup errors"
      pattern: "Local:.*localhost"
---

<objective>
Verify the complete Phase 1 foundation: dev server runs cleanly, Tailwind utility classes render, scaffold is gone, and Inter font loads. This is a human verification checkpoint — the user confirms with their eyes in the browser.

Purpose: Automated tests cannot verify visual rendering or font loading. This checkpoint catches any misconfiguration before Phase 2 builds on this foundation.
Output: Confirmed green foundation — Phase 2 can proceed.
</objective>

<execution_context>
@/Users/luis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-styling-setup/01-01-SUMMARY.md
@.planning/phases/01-foundation-styling-setup/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run dev server and add Tailwind smoke test</name>
  <files>src/App.jsx</files>
  <action>
Step 1 — Temporarily add a visible Tailwind test element to App.jsx so the human can confirm Tailwind is rendering. Update src/App.jsx to:

```jsx
function App() {
  return (
    <main className="min-h-screen bg-white text-black p-8">
      <p className="text-4xl font-bold font-sans text-near-black">
        Tailwind OK — Phase 1 Foundation
      </p>
      <p className="text-base font-sans text-gray-500 mt-2">
        Inter font + Tailwind CSS v3 active
      </p>
    </main>
  );
}

export default App;
```

Step 2 — Start the dev server:

```bash
npm run dev
```

Confirm the server starts without errors. The terminal should show the local URL (typically http://localhost:5173).

Step 3 — Report what the browser should show:
- White background (not dark/gray)
- Large bold text "Tailwind OK — Phase 1 Foundation" in Inter font
- Smaller gray text "Inter font + Tailwind CSS v3 active" below it
- No Vite scaffold (no counter, no logos, no "Vite + React" heading)

If any errors appear in the terminal, diagnose them using the pitfalls documented in 01-RESEARCH.md before surfacing to the human verifier.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Dev server starts with zero errors. npm run build exits 0. Smoke test element is visible in App.jsx.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification in browser</name>
  <action>Present the verification steps to the user and wait for approval before proceeding.</action>
  <verify>
    <automated>echo "Checkpoint: awaiting human approval"</automated>
  </verify>
  <done>Human has confirmed correct visual rendering and typed "approved".</done>
  <what-built>
    Complete Phase 1 Foundation:
    - Tailwind CSS v3 installed via PostCSS pipeline
    - Design tokens configured (Inter font, off-white, near-black, 9xl/10xl sizes)
    - src/index.css replaced with Tailwind directives only
    - src/App.jsx replaced with minimal portfolio shell
    - Inter font self-hosted via @fontsource/inter (400, 500, 700)
    - App.css and react.svg deleted
  </what-built>
  <how-to-verify>
    1. Open http://localhost:5173 in your browser
    2. Confirm: white background (not dark gray)
    3. Confirm: large bold text "Tailwind OK — Phase 1 Foundation" renders as large, bold, black text
    4. Confirm: the text appears to be in Inter (clean, geometric sans-serif) not the system fallback
    5. Confirm: no Vite scaffold visible (no spinning React logo, no counter button, no "Vite + React" heading)
    6. Confirm: no blue links visible anywhere
    7. Open browser DevTools Console — confirm zero errors
    8. Open browser DevTools Network tab — confirm @fontsource/inter font files are loading (woff2 files)
  </how-to-verify>
  <resume-signal>
    If everything looks correct: type "approved"
    If something is wrong: describe what you see (e.g., "still dark background", "text not bold", "console error: ...")
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Remove smoke test, restore clean App.jsx shell</name>
  <files>src/App.jsx</files>
  <action>
After the human approves, remove the temporary smoke test element from App.jsx. Restore the clean minimal shell:

```jsx
function App() {
  return (
    <main className="min-h-screen bg-white text-black">
      {/* Portfolio sections will be added in Phase 2+ */}
    </main>
  );
}

export default App;
```

The smoke test paragraph elements ("Tailwind OK") must not remain in the final file — they are verification only, not portfolio content.
  </action>
  <verify>
    <automated>node -e "import('fs').then(fs => { const c = fs.readFileSync('src/App.jsx', 'utf8'); const ok = !c.includes('Tailwind OK') && c.includes('min-h-screen') && c.includes('<main'); console.log(ok ? 'PASS' : 'FAIL: smoke test not removed or main element missing'); process.exit(ok ? 0 : 1); })"</automated>
  </verify>
  <done>src/App.jsx contains only the minimal shell. No smoke test content remains. Phase 1 foundation is complete and verified.</done>
</task>

</tasks>

<verification>
Phase 1 complete when:
1. `npm run build` exits with code 0 and no errors
2. Human confirmed correct visual rendering in browser
3. src/App.jsx restored to clean minimal shell (no smoke test content)
4. All Phase 1 requirements satisfied:
   - REQ-001: Tailwind CSS v3 installed with PostCSS
   - REQ-002: Design tokens configured (Inter, off-white, near-black, 9xl/10xl)
   - REQ-003: Vite scaffold replaced (App.jsx, App.css, index.css all updated/deleted)
   - REQ-004: Minimal satisfaction — main semantic root ready for anchor section IDs in Phase 2
</verification>

<success_criteria>
- `npm run build` exits 0
- Browser renders white background with Inter font (not dark Vite scaffold)
- No console errors in browser DevTools
- src/App.jsx is the clean minimal shell with no scaffold or smoke test content
- Human has typed "approved" to confirm visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-styling-setup/01-03-SUMMARY.md` documenting:
- Build output (zero errors confirmed)
- Human verification result
- Final state of all Phase 1 files
- Any issues found and resolved during verification
</output>
