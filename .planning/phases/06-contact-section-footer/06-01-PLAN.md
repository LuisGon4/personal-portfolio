---
phase: 06-contact-section-footer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sections/Contact.jsx
autonomous: false
requirements:
  - REQ-050
  - REQ-051
  - REQ-052
  - REQ-053

must_haves:
  truths:
    - "GitHub link renders with Github icon and opens in a new tab"
    - "LinkedIn link renders with Linkedin icon and opens in a new tab"
    - "Email link renders with Mail icon and uses the mailto: protocol"
    - "All three links use the established underline link style"
    - "Contact section shares the same container and heading pattern as other sections"
  artifacts:
    - path: "src/sections/Contact.jsx"
      provides: "Complete contact section with three icon links"
      exports: ["default Contact"]
      contains: "lucide-react"
  key_links:
    - from: "src/sections/Contact.jsx"
      to: "lucide-react"
      via: "named import { Github, Linkedin, Mail }"
      pattern: "import.*Github.*Linkedin.*Mail.*lucide-react"
    - from: "src/sections/Contact.jsx"
      to: "App.jsx"
      via: "existing import — no change needed"
      pattern: "import Contact from"
---

<objective>
Replace the Contact.jsx stub with a complete Contact section containing GitHub, LinkedIn, and email links — each with a lucide-react icon — using the established section patterns and black-and-white design.

Purpose: Phase 6 delivers the final content section so all four portfolio sections (About, Projects, Resume, Contact) are complete and the footer already present in Layout.jsx provides the page bottom.

Output: A complete src/sections/Contact.jsx. No other files change.
</objective>

<execution_context>
@/Users/luis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Established patterns from prior phases. Use these exactly — no codebase exploration needed. -->

Container pattern (all sections):
```jsx
<section id="sectionId" className="px-6 py-24 max-w-5xl mx-auto">
```

Heading pattern (secondary sections — Projects, Resume, Contact):
```jsx
<h2 className="text-4xl font-bold tracking-tighter mb-12">Section Name</h2>
```

Link style pattern (from index.css / established convention):
```jsx
className="underline underline-offset-4 hover:text-black/60 transition-colors"
```

lucide-react icon usage (install first, then import):
```jsx
import { Github, Linkedin, Mail } from 'lucide-react';
// Render: <Github size={20} />
```

Layout.jsx footer (already exists — DO NOT modify):
```jsx
<footer className="border-t border-black mt-24 py-8 text-center text-xs tracking-widest uppercase text-black/50">
  © {new Date().getFullYear()} Luis
</footer>
```

App.jsx (already imports and renders Contact — DO NOT modify):
```jsx
import Contact from './sections/Contact';
// <Contact /> already in JSX tree
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-react</name>
  <files>package.json</files>
  <action>
    Run `npm install lucide-react` in the project root. This adds the icon library as a production dependency. Verify it appears in package.json dependencies after install.

    Do not install any other icon library. Do not use inline SVG. lucide-react is the chosen library per the planning context.
  </action>
  <verify>
    <automated>node -e "require('./node_modules/lucide-react/dist/cjs/lucide-react.js'); console.log('ok')" 2>/dev/null || node -e "import('./node_modules/lucide-react').then(()=>console.log('ok'))"</automated>
  </verify>
  <done>lucide-react entry appears in package.json dependencies and the module resolves in node_modules.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Contact.jsx stub with complete section</name>
  <files>src/sections/Contact.jsx</files>
  <action>
    Overwrite src/sections/Contact.jsx entirely with the following implementation:

    - Import `{ Github, Linkedin, Mail }` from `lucide-react`
    - Section element: `id="contact"`, container class `px-6 py-24 max-w-5xl mx-auto`
    - h2 heading: text "Contact", class `text-4xl font-bold tracking-tighter mb-12`
    - Three link rows in a flex column (`flex flex-col gap-6`):

      1. GitHub link
         - `href="https://github.com/yourusername"`
         - `target="_blank" rel="noopener noreferrer"`
         - `<Github size={20} />` icon + span text "GitHub"
         - Link class: `inline-flex items-center gap-3 underline underline-offset-4 hover:text-black/60 transition-colors`

      2. LinkedIn link
         - `href="https://linkedin.com/in/yourusername"`
         - `target="_blank" rel="noopener noreferrer"`
         - `<Linkedin size={20} />` icon + span text "LinkedIn"
         - Same link class as above

      3. Email link
         - `href="mailto:your@email.com"`
         - NO `target="_blank"` (mailto: links open the OS mail client — new tab is not applicable for mailto:)
         - `<Mail size={20} />` icon + span text "your@email.com"
         - Same link class as above

    Design rules:
    - Black and white only — no color classes, no gradients
    - No animations beyond the `transition-colors` already in the link class
    - Remove the stub's min-h-screen — py-24 is sufficient (consistent with Projects and Resume sections)
  </action>
  <verify>
    <automated>cd /Users/luis/Desktop/PortfolioWebsite/PersonalPortfolio && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    src/sections/Contact.jsx exports a default React component that:
    - Renders a section with id="contact"
    - Contains an h2 with text "Contact"
    - Renders three links (GitHub, LinkedIn, email) each with a lucide-react icon
    - GitHub and LinkedIn links have target="_blank" rel="noopener noreferrer"
    - Email link uses href="mailto:..."
    - `npm run build` exits 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify Contact section in browser</name>
  <action>Human verification — Claude has automated everything above. This step confirms the result in the browser.</action>
  <what-built>
    Complete Contact section with GitHub, LinkedIn, and email links — each with a lucide-react icon. Footer was already present in Layout.jsx and is unchanged.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173
    2. Scroll to the bottom of the page (or click "Contact" in the nav)
    3. Confirm the Contact section heading "Contact" is visible
    4. Confirm three rows appear: a GitHub row (Github icon + "GitHub"), a LinkedIn row (Linkedin icon + "LinkedIn"), an email row (Mail icon + "your@email.com")
    5. Click the GitHub link — confirm it opens https://github.com/yourusername in a new tab
    6. Click the LinkedIn link — confirm it opens https://linkedin.com/in/yourusername in a new tab
    7. Click the email link — confirm it opens your OS mail client (no new tab)
    8. Confirm footer "© 2026 Luis" is visible below the Contact section
    9. Confirm no colors other than black/white/gray appear anywhere in the Contact section
  </how-to-verify>
  <verify>Human visual confirmation</verify>
  <done>Human has confirmed all three links render with icons, open correctly, and the footer is visible.</done>
  <resume-signal>Type "approved" to continue, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (no JSX errors)
- Contact section visible in browser at bottom of page
- Three icon-links render (Github, Linkedin, Mail icons from lucide-react)
- GitHub and LinkedIn open new tab; email opens mail client
- Footer "© 2026 Luis" present below Contact section
- No colors other than black, white, gray
</verification>

<success_criteria>
1. lucide-react installed and resolves as a module
2. src/sections/Contact.jsx replaced — no more stub placeholder text
3. GitHub link: icon + label, target="_blank", rel="noopener noreferrer"
4. LinkedIn link: icon + label, target="_blank", rel="noopener noreferrer"
5. Email link: icon + label, href="mailto:...", no target="_blank"
6. Visual style consistent with Projects and Resume sections (same container, same heading size)
7. Human verified in browser
</success_criteria>

<output>
After completion, create `.planning/phases/06-contact-section-footer/06-01-SUMMARY.md` using the summary template at @/Users/luis/.claude/get-shit-done/templates/summary.md
</output>
