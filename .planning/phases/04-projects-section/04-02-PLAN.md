---
phase: 04-projects-section
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/sections/Projects.jsx
autonomous: false
requirements:
  - REQ-030
  - REQ-031
  - REQ-032
  - REQ-033
  - REQ-034

must_haves:
  truths:
    - "The Projects section replaces the placeholder stub with real content"
    - "Three project cards render in a responsive grid (1 col mobile, 2 col tablet, 3 col desktop)"
    - "Clicking the GitHub link on any card opens the URL in a new tab"
    - "The second card renders no Live Demo link (liveUrl absent) while the others do"
    - "The Projects section is visually consistent with the About section (same container width, padding, typography scale)"
  artifacts:
    - path: "src/sections/Projects.jsx"
      provides: "Section component mapping projects data through ProjectCard in a responsive grid"
      exports: ["default Projects"]
  key_links:
    - from: "src/sections/Projects.jsx"
      to: "src/data/projects.js"
      via: "import projects from '../data/projects'"
      pattern: "import projects"
    - from: "src/sections/Projects.jsx"
      to: "src/components/ProjectCard.jsx"
      via: "import ProjectCard from '../components/ProjectCard'"
      pattern: "import ProjectCard"
    - from: "Nav.jsx href=#projects"
      to: "src/sections/Projects.jsx section id=projects"
      via: "anchor scroll"
      pattern: "id=\"projects\""
---

<objective>
Replace the Projects.jsx stub with a complete section that maps the projects data array through ProjectCard in a responsive CSS Grid, then perform human visual verification.

Purpose: This is the final wiring step — connecting the data module and card component created in Plan 01 into a rendered, navigable section.
Output: src/sections/Projects.jsx (complete), human-confirmed visual output
</objective>

<execution_context>
@/Users/luis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-projects-section/04-RESEARCH.md
@.planning/phases/04-projects-section/04-01-SUMMARY.md

<interfaces>
<!-- Prior plan output and established patterns. Executor needs these to wire correctly. -->

From src/data/projects.js (created in Plan 01):
```javascript
// Default export — array of 3 objects
[
  { title, description, tags: string[], githubUrl, liveUrl },  // entry 0: has liveUrl
  { title, description, tags: string[], githubUrl },            // entry 1: NO liveUrl
  { title, description, tags: string[], githubUrl, liveUrl },  // entry 2: has liveUrl
]
```

From src/components/ProjectCard.jsx (created in Plan 01):
```jsx
// Spread the project object — all prop names match data field names exactly
export default function ProjectCard({ title, description, tags, githubUrl, liveUrl })
// Use as: <ProjectCard key={project.title} {...project} />
```

From src/sections/About.jsx (established container pattern to match):
```jsx
<section
  id="about"
  className="min-h-screen flex flex-col justify-center px-6 py-24 max-w-5xl mx-auto"
>
  <h1 className="text-6xl md:text-9xl font-bold tracking-tighter leading-none mb-6">
```

Nav anchor that must match section id:
```jsx
{ label: 'Projects', href: '#projects' }  // section id MUST be "projects"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Projects.jsx stub with complete section</name>
  <files>src/sections/Projects.jsx</files>
  <action>
Overwrite src/sections/Projects.jsx entirely. The current stub (a placeholder paragraph) is replaced by the complete section.

Exact implementation:

```jsx
import projects from '../data/projects';
import ProjectCard from '../components/ProjectCard';

export default function Projects() {
  return (
    <section id="projects" className="px-6 py-24 max-w-5xl mx-auto">
      <h2 className="text-4xl font-bold tracking-tighter mb-12">Projects</h2>
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {projects.map((project) => (
          <ProjectCard key={project.title} {...project} />
        ))}
      </div>
    </section>
  );
}
```

Critical constraints — do not deviate:
- Section id is `"projects"` — must match Nav.jsx anchor `href="#projects"` exactly (confirmed in Nav.jsx line 5)
- Container classes `px-6 py-24 max-w-5xl mx-auto` — match About.jsx exactly for visual consistency
- Do NOT use `min-h-screen` on the Projects section — card grid height is content-driven
- Heading: `text-4xl font-bold tracking-tighter mb-12` — scaled down from About.jsx's text-6xl
- Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` — mobile-first responsive collapse
- `{...project}` spread passes all data fields; `key={project.title}` is stable for a static 3-item list
- No additional wrappers, no extra state, no useEffect — this is a pure render
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - src/sections/Projects.jsx no longer contains "coming in Phase 4" placeholder text
    - File imports both projects data and ProjectCard component
    - npm run build exits 0
    - Section id is "projects"
    - Grid class string "grid-cols-1 md:grid-cols-2 lg:grid-cols-3" is present in file
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification</name>
  <what-built>
    Three project cards rendered in a responsive grid within the Projects section. Cards show title, description, tag pills, GitHub link, and (for two of the three cards) a Live Demo link. The middle card has no Live Demo link.
  </what-built>
  <how-to-verify>
    1. Run the dev server: `npm run dev`
    2. Open http://localhost:5173 in the browser
    3. Click "Projects" in the nav — confirm smooth scroll lands on the Projects section
    4. Confirm three cards are visible with titles "Project One", "Project Two", "Project Three"
    5. Confirm each card shows: title, description text, at least one tag pill, a "GitHub" link
    6. Confirm "Project Two" card shows NO "Live Demo" link (the other two cards should show it)
    7. Click a GitHub link — confirm it opens in a new tab
    8. Resize the browser to mobile width (< 768px) — confirm cards stack to a single column
    9. Resize to tablet width (768px–1023px) — confirm two-column layout
    10. Resize to desktop width (1024px+) — confirm three-column layout

    Expected appearance:
    - Cards have a solid black border, white background, no color fill on tags
    - Tag pills are in a smaller monospace font with a light black border
    - No gradients, shadows, or colored elements
    - Section heading "Projects" visible above the grid
    - Visual alignment consistent with the About section above it (same left margin, same container width)
  </how-to-verify>
  <resume-signal>Type "approved" if the output matches expectations, or describe any visual or functional issues to fix</resume-signal>
</task>

</tasks>

<verification>
Post-execution automated checks:
1. `npm run build` exits 0
2. `grep -n 'id="projects"' src/sections/Projects.jsx` — returns match on section element
3. `grep -n 'grid-cols-1 md:grid-cols-2 lg:grid-cols-3' src/sections/Projects.jsx` — returns match
4. `grep -n 'import projects' src/sections/Projects.jsx` — returns match
5. `grep -n 'import ProjectCard' src/sections/Projects.jsx` — returns match
6. `grep -c 'coming in Phase 4' src/sections/Projects.jsx` — returns 0 (placeholder removed)
</verification>

<success_criteria>
- Projects section renders 3 cards from the data module
- Nav "Projects" link scrolls to the section
- Responsive grid: 1 col (mobile) → 2 col (tablet) → 3 col (desktop)
- Second card renders no Live Demo link
- All cards visually consistent with the site's black/white editorial design
- Human approves visual output
</success_criteria>

<output>
After completion, create .planning/phases/04-projects-section/04-02-SUMMARY.md
</output>
