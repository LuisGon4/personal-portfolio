---
wave: 1
depends_on: []
files_modified:
  - src/components/Nav.jsx
  - src/App.jsx
  - src/sections/Skills.jsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04
---

# Phase 9 Plan — Black Nav & Skills Section

## Task 1 — Invert Nav to black theme

**File:** `src/components/Nav.jsx`

1. `navLinks` array: insert `{ label: 'Skills', href: '#skills' }` between About and Projects
2. `<header>` classes: `bg-white border-b border-black` → `bg-black border-b border-white/10`
3. Logo `<a>` classes: add `text-white`
4. Desktop link `<a>` classes: add `text-white` (keep `hover:underline underline-offset-4`)
5. Hamburger `<span>` lines: `bg-black` → `bg-white` (all three spans)
6. Mobile menu `<ul>` border: `border-black` → `border-white/10`
7. Mobile menu `<li>` border: `border-black` → `border-white/10`
8. Mobile link `<a>` classes: add `text-white`, change `hover:bg-black hover:text-white` → `hover:bg-white hover:text-black`

## Task 2 — Create Skills section

**File:** `src/sections/Skills.jsx` (new file)

Structure:
- `<section id="skills" className="px-6 py-24 bg-white">`
- Inner `<div className="max-w-5xl mx-auto">`
- Section heading: `<h2 className="text-4xl font-bold tracking-tighter mb-12 flex items-center gap-3">` with indigo accent bar `<span className="w-2 h-8 bg-indigo-500 inline-block" />` then "Technologies"
- Three category blocks stacked, each:
  - Category label: `<h3 className="text-xs font-semibold tracking-widest uppercase text-black/40 mb-3">{category}</h3>`
  - Pills `<ul className="flex flex-wrap gap-2 mb-8">` with `<li>` items:
    `font-mono text-xs px-2 py-1 border border-black/30 tracking-wide hover:border-indigo-400 hover:text-indigo-600 transition-colors`

Content (hardcoded):
- **Languages:** Java, C++, SQL, JavaScript, HTML, CSS
- **Frameworks:** React, Spring Boot, PostgreSQL
- **Tools:** Git, GitHub, IntelliJ IDEA, VS Code, Postman, Docker, AWS (Basics), Vercel, Render

## Task 3 — Wire Skills into App

**File:** `src/App.jsx`

1. Add import: `import Skills from './sections/Skills';`
2. Insert `<Skills />` between `<About />` and `<Projects />`

---

## Verification

```yaml
must_haves:
  - Nav renders with black background and white text at all viewport widths
  - Mobile hamburger opens a black-background menu; links have white text and invert on hover
  - "Skills" nav link appears and smooth-scrolls to #skills
  - Technologies section exists between About and Projects in the DOM
  - Three category headings: Languages, Frameworks, Tools
  - Pills use font-mono, border, indigo hover style
  - npm run build exits 0
```
