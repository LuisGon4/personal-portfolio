---
phase: 10-hero-geometric-shapes-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sections/About.jsx
autonomous: true
requirements:
  - SHAPE-01
  - SHAPE-02
  - SHAPE-03

must_haves:
  truths:
    - "No shape div's bounding box intersects any h1 or p element at 375px viewport"
    - "No shape div's bounding box intersects any h1 or p element at 768px viewport"
    - "No shape div's bounding box intersects any h1 or p element at 1280px viewport"
    - "Hero section contains at least 4 distinct decorative shape geometries (circle, filled rectangle, horizontal line, dot)"
    - "Every decorative shape div has aria-hidden='true' and the pointer-events-none class"
    - "npm run build exits 0 with no errors"
  artifacts:
    - path: "src/sections/About.jsx"
      provides: "Hero section with 5 safe, aria-hidden decorative shapes"
      contains: "absolute top-0 right-0"
  key_links:
    - from: "src/sections/About.jsx shape divs"
      to: "section#about"
      via: "direct children of the relative section element"
      pattern: "absolute.*pointer-events-none"
    - from: "section#about"
      to: "overflow-hidden boundary"
      via: "overflow-hidden class on the section"
      pattern: "overflow-hidden"
---

<objective>
Fix the two existing decorative shapes in the hero section that overlap text at narrow viewports, then add three new shapes to meet the 4-shape minimum and provide visual variety.

Purpose: SHAPE-01 (no overlap), SHAPE-02 (4+ varied shapes), and SHAPE-03 (aria-hidden + pointer-events-none) are all addressed in a single file.
Output: src/sections/About.jsx with 5 decorative shapes, all safely positioned, accessible, and non-interactive.
</objective>

<execution_context>
@/Users/luis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hero-geometric-shapes-rework/10-RESEARCH.md

<interfaces>
<!-- Current About.jsx shape divs (the two elements being fixed) -->
<!-- Source: src/sections/About.jsx, lines 9-17 -->

Existing shape 1 (BUGGY — overlaps text at 375px):
```jsx
<div
  aria-hidden="true"
  className="absolute -top-32 -right-32 w-96 h-96 rounded-full border border-indigo-200 opacity-60 pointer-events-none"
/>
```
Problem: -right-32 (128px) + w-96 (384px) at 375px viewport places left edge of circle at 375-128-384 = -137px, covering all text.

Existing shape 2 (BUGGY — partially clips into text at 375px):
```jsx
<div
  aria-hidden="true"
  className="absolute bottom-24 -left-8 w-24 h-24 bg-indigo-50 pointer-events-none"
/>
```
Problem: -left-8 (-32px) overlaps the px-6 (24px) text padding zone.

Section wrapper (do not change):
```jsx
<section
  id="about"
  className="relative min-h-screen flex flex-col justify-center px-6 py-24 max-w-5xl mx-auto overflow-hidden"
>
```
The overflow-hidden on the section clips shapes at the boundary — this is the containment mechanism. All shape divs must be direct children of this section, not nested inside the grid div.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the two existing shapes with safe inset positioning</name>
  <files>src/sections/About.jsx</files>
  <action>
Replace the two existing decorative shape divs in src/sections/About.jsx with corrected versions that use safe inset positioning instead of negative offsets.

Shape 1 fix — large circle, top-right. Change from `-top-32 -right-32 w-96 h-96 opacity-60` to `top-0 right-0 w-48 h-48 sm:w-64 sm:h-64 md:w-96 md:h-96 opacity-40`. Moving to top-0/right-0 places the circle in the corner where overflow-hidden clips it to a quarter-circle arc — this is intentional and visually clean. The responsive sizing (w-48 at mobile, w-64 at sm, w-96 at md) ensures the circle stays in the corner at all breakpoints without crossing into the text column.

Shape 2 fix — small filled square, bottom-left. Change from `bottom-24 -left-8 w-24 h-24` to `bottom-16 left-4 w-20 h-20`. Replacing -left-8 with left-4 moves the square fully inside the section boundary. Reducing from w-24 to w-20 gives additional clearance from the text left padding.

Both shapes must retain `aria-hidden="true"` and `pointer-events-none`. Do not change any other part of the file in this task.

Final classes for shape 1:
`absolute top-0 right-0 w-48 h-48 sm:w-64 sm:h-64 md:w-96 md:h-96 rounded-full border border-indigo-200 opacity-40 pointer-events-none`

Final classes for shape 2:
`absolute bottom-16 left-4 w-20 h-20 bg-indigo-50 pointer-events-none`
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build exits 0. The two existing shape divs no longer use negative left/right offsets. grep confirms: `grep -n "\-right-32\|\-left-8" src/sections/About.jsx` returns no matches.</done>
</task>

<task type="auto">
  <name>Task 2: Add three new decorative shapes for visual variety</name>
  <files>src/sections/About.jsx</files>
  <action>
Add three new decorative shape divs as direct children of the `<section id="about">` element, placed immediately after the two existing (now fixed) shapes and before the grid div. All three must be `absolute`, `aria-hidden="true"`, and `pointer-events-none`.

Shape 3 — horizontal hairline rule, upper-left area. Place at `top-16 left-0`, sized `w-32 h-px`. This puts it 64px from the top, above the text content which begins around py-24 (96px). The 1px height cannot obscure text.
```jsx
<div
  aria-hidden="true"
  className="absolute top-16 left-0 w-32 h-px bg-black/10 pointer-events-none"
/>
```

Shape 4 — small dot accent, bottom-right. Place at `bottom-32 right-8`, sized `w-3 h-3`. At 12px diameter this cannot obscure any text. The indigo-300 color at 50% opacity gives a subtle accent that complements the circle border.
```jsx
<div
  aria-hidden="true"
  className="absolute bottom-32 right-8 w-3 h-3 rounded-full bg-indigo-300 opacity-50 pointer-events-none"
/>
```

Shape 5 — vertical hairline, right side. A thin rectangle rotated 90 degrees. Use `top-1/3 -right-4 w-24 h-1 bg-black/5 rotate-90`. The -right-4 is intentional here: the shape is only 4px wide (h-1) after rotation, so it barely clips the edge and overflow-hidden contains it. This adds a "lines" geometry category distinct from the circle, rectangle, dot, and horizontal line.
```jsx
<div
  aria-hidden="true"
  className="absolute top-1/3 -right-4 w-24 h-1 bg-black/5 rotate-90 pointer-events-none"
/>
```

After adding all three, the section should have 5 total decorative shape divs before the grid div. Do not touch the grid div, photo wrapper, or section divider at the bottom.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 &amp;&amp; grep -c 'aria-hidden="true"' src/sections/About.jsx</automated>
  </verify>
  <done>Build exits 0. `grep -c 'aria-hidden="true"' src/sections/About.jsx` returns 5. The 5 shape types present: rounded-full border (circle), bg-indigo-50 (filled square), h-px bg-black/10 (horizontal line), w-3 h-3 rounded-full (dot), rotate-90 h-1 (vertical hairline). `grep -n 'pointer-events-none' src/sections/About.jsx` returns 5 matching lines.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the following to confirm structural correctness:

```bash
# Build gate
npm run build

# Count aria-hidden shapes (must be 5)
grep -c 'aria-hidden="true"' src/sections/About.jsx

# Confirm pointer-events-none on all shapes (must be 5 lines)
grep -n 'pointer-events-none' src/sections/About.jsx

# Confirm no unsafe negative offsets remain on wide shapes
grep -n '\-right-32\|\-left-8' src/sections/About.jsx
```

Expected results:
- `npm run build` exits 0
- `grep -c 'aria-hidden="true"'` outputs `5`
- `grep -n 'pointer-events-none'` shows 5 lines
- `grep -n '\-right-32\|\-left-8'` shows no matches
</verification>

<success_criteria>
- src/sections/About.jsx contains exactly 5 decorative shape divs, all direct children of section#about
- Shape 1 (circle): `top-0 right-0` with responsive sizing `w-48 sm:w-64 md:w-96`
- Shape 2 (square): `left-4` (not -left-8), `w-20` (not w-24)
- Shape 3 (horizontal line): `h-px` height, `top-16 left-0`
- Shape 4 (dot): `w-3 h-3 rounded-full`, `bottom-32 right-8`
- Shape 5 (vertical hairline): `rotate-90 h-1`, `top-1/3`
- All 5 shapes have `aria-hidden="true"` and `pointer-events-none`
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/10-hero-geometric-shapes-rework/10-01-SUMMARY.md` following the summary template.
</output>
